import React from 'react';
import { type VocoderParams } from '../types';

// This component is no longer in use.

export const effectPresets: Record<string, VocoderParams> = {};

export const EffectSelector: React.FC = () => {
    return null;
};
